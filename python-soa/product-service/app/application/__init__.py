"""
Application Layer - Product Service
=====================================
Contains the concrete implementation of the domain service interface.
This layer orchestrates domain logic and infrastructure calls.

SOA Principle: Service Reusability
- Business logic is encapsulated in reusable service implementations
- The same service can be exposed via different transports (REST, gRPC, events)
"""
